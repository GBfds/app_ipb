const buttonVariants ={
    defaults:{
        width: "60%",
        height: 56,
        borderRadius: 8,
        backgroundColor: "green_800",
        elevation: 8,
        margin: "x",
        alignItems: "center",
        justifyContent: "center"
    }
}

export default buttonVariants